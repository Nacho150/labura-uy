{
  "buildCommand": "mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
